import React from "react";

// Виджет текущей даты
// Вызывается модулем Widgets

function WidgetDate( {format} ) {
    return 'Виджет выводящий текущиую дату в нужном формате';
}

export default WidgetDate;
