import React from "react";

// Виджет карты, принимает страну определнную из бразуера
// Вызывается модулем Widgets

function WidgetsBroadсasts( {title, list, countToShow} ) {
    return 'Рендерит виджет Эфир';
}

export default WidgetsBroadсasts;
