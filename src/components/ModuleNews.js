import React from "react";

// Модуль блока новостей

function ModuleNews( {tabs, newsList, newsLogo} ) {

    // Получение страны из tabs

    return (
        <>
            {/* Вывод списка "меню" - "Сейчас в СМИ, "В стране" "Рекомендуем" */}
            {/* Вывод списка новостей */}
        </>
    )
}

export default ModuleNews;
