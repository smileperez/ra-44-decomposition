import React from "react";

// Виджет погоды, принимает страну и город определнные из бразуера
// Вызывается модулем Widgets

function WidgetsWeather( {country, city} ) {
    return 'Рендерит виджэт погоды';
}

export default WidgetsWeather;
